# Markdown Vault Project Rules

## Versioning Policy
- Whenever a new feature is updated or a bug is fixed, you MUST always update and push up the version.
- Increment `app.version.code` and `app.version.name` in [gradle.properties](file:///Users/kaushal/antigravity/Markdown-Vault/gradle.properties) before committing.
